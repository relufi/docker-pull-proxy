name: Docker
on:
  push:
    branches: [ main ]
    paths:
      - 'trigger.txt'
env:
  REGISTRY_REGION: shanghai
  REGISTRY: registry.cn-${{ env.REGISTRY_REGION }}.aliyuncs.com
jobs:
  build:
    name: Pull
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v4
      with:
        sparse-checkout: |
          trigger.txt
        sparse-checkout-cone-mode: false
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ secrets.ALIYUN_REGISTRY_USER }}
        password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}
#    - name: Docker pull image
#      run: |
#        docker pull
#
#    - name: Build and push image to ACR EE
#      run: |
#        docker login -u $ALIYUN_REGISTRY_USER -p $ALIYUN_REGISTRY_PASSWORD $REGISTRY
#        sh pull.sh
#        data=`cat trigger.txt`
#
#        if [ $MM_NOTIFY_URL ];then
#          curl  -H 'Content-Type: application/json' \
#            -d "{\"msgtype\": \"text\",\"text\": {\"content\":\"同步成功 $data \"}}" \
#            $MM_NOTIFY_URL
#        else
#          echo 'MM_NOTIFY_URL not set'
#        fi
